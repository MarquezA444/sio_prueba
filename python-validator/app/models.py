# Este archivo está vacío porque actualmente no se usan modelos Pydantic
# Puedes agregar modelos aquí para validación de datos más robusta si lo necesitas
#
# Ejemplo:
# from pydantic import BaseModel, Field
#
# class SpotValidationRequest(BaseModel):
#     latitud: float = Field(ge=-90, le=90)
#     longitud: float = Field(ge=-180, le=180)
#     linea: str
#     posicion: int
#     lote: str

